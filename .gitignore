# Rust
/target

# Node (each SPA and the shared library)
node_modules/
dist/
*.log
npm-debug.log*

# IDE and OS
.idea/
.vscode/
.DS_Store
Thumbs.db

# Coverage and doc builds land beside `target`. `git add -A` swept 840 of
# these rustdoc files into a commit once, on a `git status | head -5` that
# hid them.
/target-*/
